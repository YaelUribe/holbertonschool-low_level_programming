#include <stdio.h>

/**
 *main - Entry Point
 *
 *Return: Always 0 (succes)
 */
int main (void
	  {
		  char puzz[55]= "with proper grammar, but the outcome is a piece of art,";
		  printf("%s\n, puzz");
		  return (0);
}
